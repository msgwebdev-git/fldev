2. Индекс на ticket_code — в таблицах order_items и b2b_order_items. Ускорит поиск билета.
  3. PM2 cluster mode — на Railway добавить, чтобы использовать все ядра CPU.

  Код

  4. PDF не кешируются — каждое скачивание билета генерирует PDF заново. Нужно генерировать один раз и хранить в Supabase Storage.
  5. Cron: email рассылка последовательная — если 1000 заказов ждут напоминания, отправляются по одному. Нужен pLimit(10) для параллельной
  отправки.

  Что можно не трогать сейчас

  - Redis-кеширование — overkill для текущего масштаба
  - Очередь задач (BullMQ) — пока хватает cron
  - Мониторинг — можно настроить позже через Railway metrics